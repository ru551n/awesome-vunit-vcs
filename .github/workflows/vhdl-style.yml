name: VHDL style

# Checks the VHDL against the VUnit-derived style in vsg.yaml with vsg-rs
# (https://github.com/ru551n/vsg-rs). On pull requests, layout findings are posted as
# suggested changes and a summary comment; other findings are annotations on the changed
# lines. To fix them locally:
#
#   pip install -e ".[dev]"
#   vsg-rs $(git ls-files "*.vhd" ":!:tests/python/golden") -c vsg.yaml --fix
#
# Only tracked VHDL is checked, and not the generated tests/python/golden (compared byte
# for byte). A fresh checkout has no generated VHDL, so directories suffice here; locally,
# git ls-files skips what the tests and examples generate.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  vsg:
    name: vsg-rs
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      # Suggested changes and the summary comment
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - uses: ru551n/vsg-rs@v0.9.6
        with:
          args: --recursive src/awesome_vunit_vcs/vhdl tests/vhdl examples benchmarks -c vsg.yaml
