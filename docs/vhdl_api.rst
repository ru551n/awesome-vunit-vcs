VHDL API
========

.. note::

   This page is in progress. It is written when the GMII components are complete.

The components follow the conventions of the VUnit verification components: a handle created by a
``new_*`` function, logging through VUnit loggers and checkers, and communication through ``com``.
